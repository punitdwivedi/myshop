// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.SalesChannelTranasaction;
import com.myshop.domain.SalesChannelTranasactionDataOnDemand;
import com.myshop.domain.SalesChannelTranasactionIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SalesChannelTranasactionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SalesChannelTranasactionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SalesChannelTranasactionIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SalesChannelTranasactionIntegrationTest: @Transactional;
    
    @Autowired
    SalesChannelTranasactionDataOnDemand SalesChannelTranasactionIntegrationTest.dod;
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testCountSalesChannelTranasactions() {
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", dod.getRandomSalesChannelTranasaction());
        long count = SalesChannelTranasaction.countSalesChannelTranasactions();
        Assert.assertTrue("Counter for 'SalesChannelTranasaction' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testFindSalesChannelTranasaction() {
        SalesChannelTranasaction obj = dod.getRandomSalesChannelTranasaction();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to provide an identifier", id);
        obj = SalesChannelTranasaction.findSalesChannelTranasaction(id);
        Assert.assertNotNull("Find method for 'SalesChannelTranasaction' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SalesChannelTranasaction' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testFindAllSalesChannelTranasactions() {
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", dod.getRandomSalesChannelTranasaction());
        long count = SalesChannelTranasaction.countSalesChannelTranasactions();
        Assert.assertTrue("Too expensive to perform a find all test for 'SalesChannelTranasaction', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SalesChannelTranasaction> result = SalesChannelTranasaction.findAllSalesChannelTranasactions();
        Assert.assertNotNull("Find all method for 'SalesChannelTranasaction' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SalesChannelTranasaction' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testFindSalesChannelTranasactionEntries() {
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", dod.getRandomSalesChannelTranasaction());
        long count = SalesChannelTranasaction.countSalesChannelTranasactions();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SalesChannelTranasaction> result = SalesChannelTranasaction.findSalesChannelTranasactionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SalesChannelTranasaction' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SalesChannelTranasaction' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testFlush() {
        SalesChannelTranasaction obj = dod.getRandomSalesChannelTranasaction();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to provide an identifier", id);
        obj = SalesChannelTranasaction.findSalesChannelTranasaction(id);
        Assert.assertNotNull("Find method for 'SalesChannelTranasaction' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifySalesChannelTranasaction(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'SalesChannelTranasaction' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testMergeUpdate() {
        SalesChannelTranasaction obj = dod.getRandomSalesChannelTranasaction();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to provide an identifier", id);
        obj = SalesChannelTranasaction.findSalesChannelTranasaction(id);
        boolean modified =  dod.modifySalesChannelTranasaction(obj);
        Integer currentVersion = obj.getVersion();
        SalesChannelTranasaction merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'SalesChannelTranasaction' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", dod.getRandomSalesChannelTranasaction());
        SalesChannelTranasaction obj = dod.getNewTransientSalesChannelTranasaction(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'SalesChannelTranasaction' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'SalesChannelTranasaction' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SalesChannelTranasactionIntegrationTest.testRemove() {
        SalesChannelTranasaction obj = dod.getRandomSalesChannelTranasaction();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SalesChannelTranasaction' failed to provide an identifier", id);
        obj = SalesChannelTranasaction.findSalesChannelTranasaction(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'SalesChannelTranasaction' with identifier '" + id + "'", SalesChannelTranasaction.findSalesChannelTranasaction(id));
    }
    
}
