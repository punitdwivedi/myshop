// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.PaymentTerm;
import com.myshop.domain.PaymentTermDataOnDemand;
import com.myshop.domain.PaymentTermIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PaymentTermIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PaymentTermIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PaymentTermIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PaymentTermIntegrationTest: @Transactional;
    
    @Autowired
    PaymentTermDataOnDemand PaymentTermIntegrationTest.dod;
    
    @Test
    public void PaymentTermIntegrationTest.testCountPaymentTerms() {
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", dod.getRandomPaymentTerm());
        long count = PaymentTerm.countPaymentTerms();
        Assert.assertTrue("Counter for 'PaymentTerm' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PaymentTermIntegrationTest.testFindPaymentTerm() {
        PaymentTerm obj = dod.getRandomPaymentTerm();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to provide an identifier", id);
        obj = PaymentTerm.findPaymentTerm(id);
        Assert.assertNotNull("Find method for 'PaymentTerm' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PaymentTerm' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PaymentTermIntegrationTest.testFindAllPaymentTerms() {
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", dod.getRandomPaymentTerm());
        long count = PaymentTerm.countPaymentTerms();
        Assert.assertTrue("Too expensive to perform a find all test for 'PaymentTerm', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PaymentTerm> result = PaymentTerm.findAllPaymentTerms();
        Assert.assertNotNull("Find all method for 'PaymentTerm' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PaymentTerm' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PaymentTermIntegrationTest.testFindPaymentTermEntries() {
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", dod.getRandomPaymentTerm());
        long count = PaymentTerm.countPaymentTerms();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PaymentTerm> result = PaymentTerm.findPaymentTermEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PaymentTerm' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PaymentTerm' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PaymentTermIntegrationTest.testFlush() {
        PaymentTerm obj = dod.getRandomPaymentTerm();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to provide an identifier", id);
        obj = PaymentTerm.findPaymentTerm(id);
        Assert.assertNotNull("Find method for 'PaymentTerm' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPaymentTerm(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'PaymentTerm' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PaymentTermIntegrationTest.testMergeUpdate() {
        PaymentTerm obj = dod.getRandomPaymentTerm();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to provide an identifier", id);
        obj = PaymentTerm.findPaymentTerm(id);
        boolean modified =  dod.modifyPaymentTerm(obj);
        Integer currentVersion = obj.getVersion();
        PaymentTerm merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'PaymentTerm' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PaymentTermIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", dod.getRandomPaymentTerm());
        PaymentTerm obj = dod.getNewTransientPaymentTerm(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PaymentTerm' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'PaymentTerm' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PaymentTermIntegrationTest.testRemove() {
        PaymentTerm obj = dod.getRandomPaymentTerm();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentTerm' failed to provide an identifier", id);
        obj = PaymentTerm.findPaymentTerm(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PaymentTerm' with identifier '" + id + "'", PaymentTerm.findPaymentTerm(id));
    }
    
}
