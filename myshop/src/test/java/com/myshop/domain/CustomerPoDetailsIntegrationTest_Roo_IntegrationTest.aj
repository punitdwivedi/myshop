// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.CustomerPoDetails;
import com.myshop.domain.CustomerPoDetailsDataOnDemand;
import com.myshop.domain.CustomerPoDetailsIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CustomerPoDetailsIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CustomerPoDetailsIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CustomerPoDetailsIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CustomerPoDetailsIntegrationTest: @Transactional;
    
    @Autowired
    CustomerPoDetailsDataOnDemand CustomerPoDetailsIntegrationTest.dod;
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testCountCustomerPoDetailses() {
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", dod.getRandomCustomerPoDetails());
        long count = CustomerPoDetails.countCustomerPoDetailses();
        Assert.assertTrue("Counter for 'CustomerPoDetails' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testFindCustomerPoDetails() {
        CustomerPoDetails obj = dod.getRandomCustomerPoDetails();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to provide an identifier", id);
        obj = CustomerPoDetails.findCustomerPoDetails(id);
        Assert.assertNotNull("Find method for 'CustomerPoDetails' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CustomerPoDetails' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testFindAllCustomerPoDetailses() {
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", dod.getRandomCustomerPoDetails());
        long count = CustomerPoDetails.countCustomerPoDetailses();
        Assert.assertTrue("Too expensive to perform a find all test for 'CustomerPoDetails', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CustomerPoDetails> result = CustomerPoDetails.findAllCustomerPoDetailses();
        Assert.assertNotNull("Find all method for 'CustomerPoDetails' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CustomerPoDetails' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testFindCustomerPoDetailsEntries() {
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", dod.getRandomCustomerPoDetails());
        long count = CustomerPoDetails.countCustomerPoDetailses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CustomerPoDetails> result = CustomerPoDetails.findCustomerPoDetailsEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CustomerPoDetails' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CustomerPoDetails' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testFlush() {
        CustomerPoDetails obj = dod.getRandomCustomerPoDetails();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to provide an identifier", id);
        obj = CustomerPoDetails.findCustomerPoDetails(id);
        Assert.assertNotNull("Find method for 'CustomerPoDetails' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCustomerPoDetails(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CustomerPoDetails' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testMergeUpdate() {
        CustomerPoDetails obj = dod.getRandomCustomerPoDetails();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to provide an identifier", id);
        obj = CustomerPoDetails.findCustomerPoDetails(id);
        boolean modified =  dod.modifyCustomerPoDetails(obj);
        Integer currentVersion = obj.getVersion();
        CustomerPoDetails merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CustomerPoDetails' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", dod.getRandomCustomerPoDetails());
        CustomerPoDetails obj = dod.getNewTransientCustomerPoDetails(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CustomerPoDetails' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'CustomerPoDetails' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CustomerPoDetailsIntegrationTest.testRemove() {
        CustomerPoDetails obj = dod.getRandomCustomerPoDetails();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerPoDetails' failed to provide an identifier", id);
        obj = CustomerPoDetails.findCustomerPoDetails(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CustomerPoDetails' with identifier '" + id + "'", CustomerPoDetails.findCustomerPoDetails(id));
    }
    
}
