// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.CustomerInvoice;
import com.myshop.domain.CustomerInvoiceDataOnDemand;
import com.myshop.domain.CustomerInvoiceIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CustomerInvoiceIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CustomerInvoiceIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CustomerInvoiceIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: CustomerInvoiceIntegrationTest: @Transactional;
    
    @Autowired
    CustomerInvoiceDataOnDemand CustomerInvoiceIntegrationTest.dod;
    
    @Test
    public void CustomerInvoiceIntegrationTest.testCountCustomerInvoices() {
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", dod.getRandomCustomerInvoice());
        long count = CustomerInvoice.countCustomerInvoices();
        Assert.assertTrue("Counter for 'CustomerInvoice' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testFindCustomerInvoice() {
        CustomerInvoice obj = dod.getRandomCustomerInvoice();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to provide an identifier", id);
        obj = CustomerInvoice.findCustomerInvoice(id);
        Assert.assertNotNull("Find method for 'CustomerInvoice' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'CustomerInvoice' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testFindAllCustomerInvoices() {
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", dod.getRandomCustomerInvoice());
        long count = CustomerInvoice.countCustomerInvoices();
        Assert.assertTrue("Too expensive to perform a find all test for 'CustomerInvoice', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<CustomerInvoice> result = CustomerInvoice.findAllCustomerInvoices();
        Assert.assertNotNull("Find all method for 'CustomerInvoice' illegally returned null", result);
        Assert.assertTrue("Find all method for 'CustomerInvoice' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testFindCustomerInvoiceEntries() {
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", dod.getRandomCustomerInvoice());
        long count = CustomerInvoice.countCustomerInvoices();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<CustomerInvoice> result = CustomerInvoice.findCustomerInvoiceEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'CustomerInvoice' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'CustomerInvoice' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testFlush() {
        CustomerInvoice obj = dod.getRandomCustomerInvoice();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to provide an identifier", id);
        obj = CustomerInvoice.findCustomerInvoice(id);
        Assert.assertNotNull("Find method for 'CustomerInvoice' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCustomerInvoice(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'CustomerInvoice' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testMergeUpdate() {
        CustomerInvoice obj = dod.getRandomCustomerInvoice();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to provide an identifier", id);
        obj = CustomerInvoice.findCustomerInvoice(id);
        boolean modified =  dod.modifyCustomerInvoice(obj);
        Integer currentVersion = obj.getVersion();
        CustomerInvoice merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'CustomerInvoice' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", dod.getRandomCustomerInvoice());
        CustomerInvoice obj = dod.getNewTransientCustomerInvoice(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'CustomerInvoice' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'CustomerInvoice' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CustomerInvoiceIntegrationTest.testRemove() {
        CustomerInvoice obj = dod.getRandomCustomerInvoice();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'CustomerInvoice' failed to provide an identifier", id);
        obj = CustomerInvoice.findCustomerInvoice(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'CustomerInvoice' with identifier '" + id + "'", CustomerInvoice.findCustomerInvoice(id));
    }
    
}
