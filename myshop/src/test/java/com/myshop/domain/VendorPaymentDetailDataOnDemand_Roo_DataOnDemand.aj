// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.VendorPaymentDataOnDemand;
import com.myshop.domain.VendorPaymentDetail;
import com.myshop.domain.VendorPaymentDetailDataOnDemand;
import com.myshop.domain.VendorPoDetailDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect VendorPaymentDetailDataOnDemand_Roo_DataOnDemand {
    
    declare @type: VendorPaymentDetailDataOnDemand: @Component;
    
    private Random VendorPaymentDetailDataOnDemand.rnd = new SecureRandom();
    
    private List<VendorPaymentDetail> VendorPaymentDetailDataOnDemand.data;
    
    @Autowired
    VendorPaymentDataOnDemand VendorPaymentDetailDataOnDemand.vendorPaymentDataOnDemand;
    
    @Autowired
    VendorPoDetailDataOnDemand VendorPaymentDetailDataOnDemand.vendorPoDetailDataOnDemand;
    
    public VendorPaymentDetail VendorPaymentDetailDataOnDemand.getNewTransientVendorPaymentDetail(int index) {
        VendorPaymentDetail obj = new VendorPaymentDetail();
        setQty(obj, index);
        setUnitPrice(obj, index);
        return obj;
    }
    
    public void VendorPaymentDetailDataOnDemand.setQty(VendorPaymentDetail obj, int index) {
        Double qty = new Integer(index).doubleValue();
        obj.setQty(qty);
    }
    
    public void VendorPaymentDetailDataOnDemand.setUnitPrice(VendorPaymentDetail obj, int index) {
        Double unitPrice = new Integer(index).doubleValue();
        obj.setUnitPrice(unitPrice);
    }
    
    public VendorPaymentDetail VendorPaymentDetailDataOnDemand.getSpecificVendorPaymentDetail(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        VendorPaymentDetail obj = data.get(index);
        Long id = obj.getId();
        return VendorPaymentDetail.findVendorPaymentDetail(id);
    }
    
    public VendorPaymentDetail VendorPaymentDetailDataOnDemand.getRandomVendorPaymentDetail() {
        init();
        VendorPaymentDetail obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return VendorPaymentDetail.findVendorPaymentDetail(id);
    }
    
    public boolean VendorPaymentDetailDataOnDemand.modifyVendorPaymentDetail(VendorPaymentDetail obj) {
        return false;
    }
    
    public void VendorPaymentDetailDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = VendorPaymentDetail.findVendorPaymentDetailEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'VendorPaymentDetail' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<VendorPaymentDetail>();
        for (int i = 0; i < 10; i++) {
            VendorPaymentDetail obj = getNewTransientVendorPaymentDetail(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
