// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.CustomerCreditMemo;
import com.myshop.domain.CustomerCreditMemoDataOnDemand;
import com.myshop.domain.CustomerPODataOnDemand;
import com.myshop.domain.CustomerPoDetailsDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect CustomerCreditMemoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: CustomerCreditMemoDataOnDemand: @Component;
    
    private Random CustomerCreditMemoDataOnDemand.rnd = new SecureRandom();
    
    private List<CustomerCreditMemo> CustomerCreditMemoDataOnDemand.data;
    
    @Autowired
    CustomerPODataOnDemand CustomerCreditMemoDataOnDemand.customerPODataOnDemand;
    
    @Autowired
    CustomerPoDetailsDataOnDemand CustomerCreditMemoDataOnDemand.customerPoDetailsDataOnDemand;
    
    public CustomerCreditMemo CustomerCreditMemoDataOnDemand.getNewTransientCustomerCreditMemo(int index) {
        CustomerCreditMemo obj = new CustomerCreditMemo();
        setIssueDate(obj, index);
        setRejectedNo(obj, index);
        setRejectedQty(obj, index);
        setRejectionReason(obj, index);
        setUnit(obj, index);
        setUnitPrice(obj, index);
        return obj;
    }
    
    public void CustomerCreditMemoDataOnDemand.setIssueDate(CustomerCreditMemo obj, int index) {
        Date issueDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setIssueDate(issueDate);
    }
    
    public void CustomerCreditMemoDataOnDemand.setRejectedNo(CustomerCreditMemo obj, int index) {
        Integer rejectedNo = new Integer(index);
        obj.setRejectedNo(rejectedNo);
    }
    
    public void CustomerCreditMemoDataOnDemand.setRejectedQty(CustomerCreditMemo obj, int index) {
        Double rejectedQty = new Integer(index).doubleValue();
        obj.setRejectedQty(rejectedQty);
    }
    
    public void CustomerCreditMemoDataOnDemand.setRejectionReason(CustomerCreditMemo obj, int index) {
        String rejectionReason = "rejectionReason_" + index;
        obj.setRejectionReason(rejectionReason);
    }
    
    public void CustomerCreditMemoDataOnDemand.setUnit(CustomerCreditMemo obj, int index) {
        String unit = "unit_" + index;
        obj.setUnit(unit);
    }
    
    public void CustomerCreditMemoDataOnDemand.setUnitPrice(CustomerCreditMemo obj, int index) {
        Double unitPrice = new Integer(index).doubleValue();
        obj.setUnitPrice(unitPrice);
    }
    
    public CustomerCreditMemo CustomerCreditMemoDataOnDemand.getSpecificCustomerCreditMemo(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CustomerCreditMemo obj = data.get(index);
        Long id = obj.getId();
        return CustomerCreditMemo.findCustomerCreditMemo(id);
    }
    
    public CustomerCreditMemo CustomerCreditMemoDataOnDemand.getRandomCustomerCreditMemo() {
        init();
        CustomerCreditMemo obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CustomerCreditMemo.findCustomerCreditMemo(id);
    }
    
    public boolean CustomerCreditMemoDataOnDemand.modifyCustomerCreditMemo(CustomerCreditMemo obj) {
        return false;
    }
    
    public void CustomerCreditMemoDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CustomerCreditMemo.findCustomerCreditMemoEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CustomerCreditMemo' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CustomerCreditMemo>();
        for (int i = 0; i < 10; i++) {
            CustomerCreditMemo obj = getNewTransientCustomerCreditMemo(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
