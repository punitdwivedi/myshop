// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.ProductCategory;
import com.myshop.domain.ProductCategoryDataOnDemand;
import com.myshop.domain.ProductCategoryIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProductCategoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProductCategoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProductCategoryIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProductCategoryIntegrationTest: @Transactional;
    
    @Autowired
    ProductCategoryDataOnDemand ProductCategoryIntegrationTest.dod;
    
    @Test
    public void ProductCategoryIntegrationTest.testCountProductCategorys() {
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", dod.getRandomProductCategory());
        long count = ProductCategory.countProductCategorys();
        Assert.assertTrue("Counter for 'ProductCategory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testFindProductCategory() {
        ProductCategory obj = dod.getRandomProductCategory();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to provide an identifier", id);
        obj = ProductCategory.findProductCategory(id);
        Assert.assertNotNull("Find method for 'ProductCategory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProductCategory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testFindAllProductCategorys() {
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", dod.getRandomProductCategory());
        long count = ProductCategory.countProductCategorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProductCategory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProductCategory> result = ProductCategory.findAllProductCategorys();
        Assert.assertNotNull("Find all method for 'ProductCategory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProductCategory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testFindProductCategoryEntries() {
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", dod.getRandomProductCategory());
        long count = ProductCategory.countProductCategorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProductCategory> result = ProductCategory.findProductCategoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProductCategory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProductCategory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testFlush() {
        ProductCategory obj = dod.getRandomProductCategory();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to provide an identifier", id);
        obj = ProductCategory.findProductCategory(id);
        Assert.assertNotNull("Find method for 'ProductCategory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProductCategory(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProductCategory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testMergeUpdate() {
        ProductCategory obj = dod.getRandomProductCategory();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to provide an identifier", id);
        obj = ProductCategory.findProductCategory(id);
        boolean modified =  dod.modifyProductCategory(obj);
        Integer currentVersion = obj.getVersion();
        ProductCategory merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProductCategory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", dod.getRandomProductCategory());
        ProductCategory obj = dod.getNewTransientProductCategory(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProductCategory' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ProductCategory' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProductCategoryIntegrationTest.testRemove() {
        ProductCategory obj = dod.getRandomProductCategory();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProductCategory' failed to provide an identifier", id);
        obj = ProductCategory.findProductCategory(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProductCategory' with identifier '" + id + "'", ProductCategory.findProductCategory(id));
    }
    
}
