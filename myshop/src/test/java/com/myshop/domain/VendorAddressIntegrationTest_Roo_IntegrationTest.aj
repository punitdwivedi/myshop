// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.VendorAddress;
import com.myshop.domain.VendorAddressDataOnDemand;
import com.myshop.domain.VendorAddressIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect VendorAddressIntegrationTest_Roo_IntegrationTest {
    
    declare @type: VendorAddressIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: VendorAddressIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: VendorAddressIntegrationTest: @Transactional;
    
    @Autowired
    VendorAddressDataOnDemand VendorAddressIntegrationTest.dod;
    
    @Test
    public void VendorAddressIntegrationTest.testCountVendorAddresses() {
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", dod.getRandomVendorAddress());
        long count = VendorAddress.countVendorAddresses();
        Assert.assertTrue("Counter for 'VendorAddress' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void VendorAddressIntegrationTest.testFindVendorAddress() {
        VendorAddress obj = dod.getRandomVendorAddress();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to provide an identifier", id);
        obj = VendorAddress.findVendorAddress(id);
        Assert.assertNotNull("Find method for 'VendorAddress' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'VendorAddress' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void VendorAddressIntegrationTest.testFindAllVendorAddresses() {
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", dod.getRandomVendorAddress());
        long count = VendorAddress.countVendorAddresses();
        Assert.assertTrue("Too expensive to perform a find all test for 'VendorAddress', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<VendorAddress> result = VendorAddress.findAllVendorAddresses();
        Assert.assertNotNull("Find all method for 'VendorAddress' illegally returned null", result);
        Assert.assertTrue("Find all method for 'VendorAddress' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void VendorAddressIntegrationTest.testFindVendorAddressEntries() {
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", dod.getRandomVendorAddress());
        long count = VendorAddress.countVendorAddresses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<VendorAddress> result = VendorAddress.findVendorAddressEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'VendorAddress' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'VendorAddress' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void VendorAddressIntegrationTest.testFlush() {
        VendorAddress obj = dod.getRandomVendorAddress();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to provide an identifier", id);
        obj = VendorAddress.findVendorAddress(id);
        Assert.assertNotNull("Find method for 'VendorAddress' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyVendorAddress(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'VendorAddress' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void VendorAddressIntegrationTest.testMergeUpdate() {
        VendorAddress obj = dod.getRandomVendorAddress();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to provide an identifier", id);
        obj = VendorAddress.findVendorAddress(id);
        boolean modified =  dod.modifyVendorAddress(obj);
        Integer currentVersion = obj.getVersion();
        VendorAddress merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'VendorAddress' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void VendorAddressIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", dod.getRandomVendorAddress());
        VendorAddress obj = dod.getNewTransientVendorAddress(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'VendorAddress' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'VendorAddress' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void VendorAddressIntegrationTest.testRemove() {
        VendorAddress obj = dod.getRandomVendorAddress();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'VendorAddress' failed to provide an identifier", id);
        obj = VendorAddress.findVendorAddress(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'VendorAddress' with identifier '" + id + "'", VendorAddress.findVendorAddress(id));
    }
    
}
