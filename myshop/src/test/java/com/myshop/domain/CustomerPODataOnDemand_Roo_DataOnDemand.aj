// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.AddressDataOnDemand;
import com.myshop.domain.Customer;
import com.myshop.domain.CustomerPO;
import com.myshop.domain.CustomerPODataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect CustomerPODataOnDemand_Roo_DataOnDemand {
    
    declare @type: CustomerPODataOnDemand: @Component;
    
    private Random CustomerPODataOnDemand.rnd = new SecureRandom();
    
    private List<CustomerPO> CustomerPODataOnDemand.data;
    
    @Autowired
    AddressDataOnDemand CustomerPODataOnDemand.addressDataOnDemand;
    
    public CustomerPO CustomerPODataOnDemand.getNewTransientCustomerPO(int index) {
        CustomerPO obj = new CustomerPO();
        setCustomerId(obj, index);
        setNote(obj, index);
        setOrderIsOpen(obj, index);
        return obj;
    }
    
    public void CustomerPODataOnDemand.setCustomerId(CustomerPO obj, int index) {
        Customer customerId = null;
        obj.setCustomerId(customerId);
    }
    
    public void CustomerPODataOnDemand.setNote(CustomerPO obj, int index) {
        String note = "note_" + index;
        obj.setNote(note);
    }
    
    public void CustomerPODataOnDemand.setOrderIsOpen(CustomerPO obj, int index) {
        Boolean orderIsOpen = Boolean.TRUE;
        obj.setOrderIsOpen(orderIsOpen);
    }
    
    public CustomerPO CustomerPODataOnDemand.getSpecificCustomerPO(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        CustomerPO obj = data.get(index);
        Long id = obj.getId();
        return CustomerPO.findCustomerPO(id);
    }
    
    public CustomerPO CustomerPODataOnDemand.getRandomCustomerPO() {
        init();
        CustomerPO obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return CustomerPO.findCustomerPO(id);
    }
    
    public boolean CustomerPODataOnDemand.modifyCustomerPO(CustomerPO obj) {
        return false;
    }
    
    public void CustomerPODataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = CustomerPO.findCustomerPOEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'CustomerPO' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<CustomerPO>();
        for (int i = 0; i < 10; i++) {
            CustomerPO obj = getNewTransientCustomerPO(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
