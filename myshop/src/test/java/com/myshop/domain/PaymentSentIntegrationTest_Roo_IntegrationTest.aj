// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.myshop.domain;

import com.myshop.domain.PaymentSent;
import com.myshop.domain.PaymentSentDataOnDemand;
import com.myshop.domain.PaymentSentIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PaymentSentIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PaymentSentIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PaymentSentIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PaymentSentIntegrationTest: @Transactional;
    
    @Autowired
    PaymentSentDataOnDemand PaymentSentIntegrationTest.dod;
    
    @Test
    public void PaymentSentIntegrationTest.testCountPaymentSents() {
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", dod.getRandomPaymentSent());
        long count = PaymentSent.countPaymentSents();
        Assert.assertTrue("Counter for 'PaymentSent' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PaymentSentIntegrationTest.testFindPaymentSent() {
        PaymentSent obj = dod.getRandomPaymentSent();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to provide an identifier", id);
        obj = PaymentSent.findPaymentSent(id);
        Assert.assertNotNull("Find method for 'PaymentSent' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PaymentSent' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PaymentSentIntegrationTest.testFindAllPaymentSents() {
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", dod.getRandomPaymentSent());
        long count = PaymentSent.countPaymentSents();
        Assert.assertTrue("Too expensive to perform a find all test for 'PaymentSent', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PaymentSent> result = PaymentSent.findAllPaymentSents();
        Assert.assertNotNull("Find all method for 'PaymentSent' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PaymentSent' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PaymentSentIntegrationTest.testFindPaymentSentEntries() {
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", dod.getRandomPaymentSent());
        long count = PaymentSent.countPaymentSents();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PaymentSent> result = PaymentSent.findPaymentSentEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PaymentSent' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PaymentSent' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PaymentSentIntegrationTest.testFlush() {
        PaymentSent obj = dod.getRandomPaymentSent();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to provide an identifier", id);
        obj = PaymentSent.findPaymentSent(id);
        Assert.assertNotNull("Find method for 'PaymentSent' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPaymentSent(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'PaymentSent' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PaymentSentIntegrationTest.testMergeUpdate() {
        PaymentSent obj = dod.getRandomPaymentSent();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to provide an identifier", id);
        obj = PaymentSent.findPaymentSent(id);
        boolean modified =  dod.modifyPaymentSent(obj);
        Integer currentVersion = obj.getVersion();
        PaymentSent merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'PaymentSent' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PaymentSentIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", dod.getRandomPaymentSent());
        PaymentSent obj = dod.getNewTransientPaymentSent(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PaymentSent' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'PaymentSent' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PaymentSentIntegrationTest.testRemove() {
        PaymentSent obj = dod.getRandomPaymentSent();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PaymentSent' failed to provide an identifier", id);
        obj = PaymentSent.findPaymentSent(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PaymentSent' with identifier '" + id + "'", PaymentSent.findPaymentSent(id));
    }
    
}
